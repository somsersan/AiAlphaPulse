# 🚀 Инструкция по развертыванию AiAlphaPulse на сервере

## 📋 Содержание
1. [Требования](#требования)
2. [Подготовка сервера](#подготовка-сервера)
3. [Установка Docker](#установка-docker)
4. [Подготовка проекта](#подготовка-проекта)
5. [Настройка переменных окружения](#настройка-переменных-окружения)
6. [Сборка и запуск](#сборка-и-запуск)
7. [Проверка работы](#проверка-работы)
8. [Мониторинг и логи](#мониторинг-и-логи)
9. [Обновление](#обновление)
10. [Устранение неполадок](#устранение-неполадок)

---

## 🖥️ Требования

### Минимальные требования к серверу:
- **ОС**: Ubuntu 20.04+ / Debian 11+ / CentOS 8+
- **RAM**: 4 GB (рекомендуется 8 GB)
- **CPU**: 2 ядра (рекомендуется 4+)
- **Диск**: 20 GB свободного места
- **Сеть**: Доступ к интернету для скачивания моделей и API запросов

### Программное обеспечение:
- Docker 20.10+
- Docker Compose 2.0+
- PostgreSQL 12+ (должен быть запущен на сервере или доступен по сети)
- Git

---

## 🛠️ Подготовка сервера

### 1. Обновление системы

```bash
# Ubuntu/Debian
sudo apt update && sudo apt upgrade -y

# CentOS/RHEL
sudo yum update -y
```

### 2. Установка необходимых пакетов

```bash
# Ubuntu/Debian
sudo apt install -y git curl wget

# CentOS/RHEL
sudo yum install -y git curl wget
```

---

## 🐳 Установка Docker

### Ubuntu/Debian

```bash
# Удаляем старые версии (если есть)
sudo apt remove docker docker-engine docker.io containerd runc

# Устанавливаем зависимости
sudo apt install -y \
    ca-certificates \
    curl \
    gnupg \
    lsb-release

# Добавляем официальный GPG ключ Docker
sudo mkdir -p /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

# Добавляем репозиторий Docker
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
  $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# Устанавливаем Docker
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

# Запускаем Docker
sudo systemctl start docker
sudo systemctl enable docker

# Проверяем установку
sudo docker --version
sudo docker compose version
```

### CentOS/RHEL

```bash
# Устанавливаем зависимости
sudo yum install -y yum-utils

# Добавляем репозиторий Docker
sudo yum-config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo

# Устанавливаем Docker
sudo yum install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

# Запускаем Docker
sudo systemctl start docker
sudo systemctl enable docker

# Проверяем установку
sudo docker --version
sudo docker compose version
```

### Настройка пользователя для Docker (опционально, но рекомендуется)

```bash
# Добавляем текущего пользователя в группу docker
sudo usermod -aG docker $USER

# Применяем изменения (или перелогиниваемся)
newgrp docker

# Проверяем работу без sudo
docker ps
```

---

## 📁 Подготовка проекта

### 1. Клонирование репозитория

```bash
# Переходим в домашнюю директорию или создаем директорию для проектов
cd ~
mkdir -p projects
cd projects

# Клонируем репозиторий
git clone <your-repo-url> AiAlphaPulse
cd AiAlphaPulse

# Проверяем ветку
git branch
```

### 2. Создание необходимых директорий

```bash
# Создаем директорию для логов (если её нет)
mkdir -p logs

# Устанавливаем правильные права
chmod 755 logs
```

---

## ⚙️ Настройка переменных окружения

### 1. Создание файла .env

```bash
# Копируем пример .env (если есть) или создаем новый
cp .env.example .env  # если файл существует
# или
touch .env
```

### 2. Редактирование .env файла

```bash
nano .env
```

### 3. Минимальная конфигурация .env

```env
# ============================================================================
# PostgreSQL подключение
# ============================================================================
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=alphapulse
POSTGRES_USER=admin
POSTGRES_PASSWORD=your-secure-password-here

# ============================================================================
# ProxyAPI.ru - ОБЯЗАТЕЛЬНО для работы LLM анализа
# Получить ключ: https://console.proxyapi.ru
# ============================================================================
PROXYAPI_KEY=your-proxyapi-key-here

# ============================================================================
# LLM модели
# ============================================================================
LLM_MODEL=anthropic/claude-3.5-haiku
LLM_ANALYSIS_MODEL=anthropic/claude-3.5-sonnet
LLM_DELAY=1.0

# ============================================================================
# Настройки пайплайна обработки
# ============================================================================
# Интервал проверки новых статей (в секундах)
PIPELINE_CHECK_INTERVAL=300

# Размер батча для обработки
PIPELINE_BATCH_SIZE=100

# Лимит статей для LLM анализа за цикл
PIPELINE_LLM_LIMIT=50

# ============================================================================
# Telegram Bot - ОБЯЗАТЕЛЬНО для работы бота
# Получить токен: https://t.me/BotFather
# ============================================================================
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here
TELEGRAM_CHAT_ID=  # Опционально, для обратной совместимости

# Настройки мониторинга горячих новостей
HOT_NEWS_THRESHOLD=0.7
HOT_NEWS_INTERVAL=60
```

### 4. Установка прав доступа к .env

```bash
# Ограничиваем доступ к файлу с секретами
chmod 600 .env
```

---

## 🔨 Сборка и запуск

### 1. Проверка конфигурации

```bash
# Проверяем docker-compose.yml
cat docker-compose.yml

# Проверяем наличие .env файла
ls -la .env
```

### 2. Сборка Docker образов

```bash
# Собираем образы (это займет несколько минут при первом запуске)
docker compose build

# Или с подробным выводом
docker compose build --progress=plain
```

**Примечание**: Первая сборка может занять 10-15 минут из-за:
- Скачивания базового образа Python
- Установки системных зависимостей
- Установки Python пакетов
- Скачивания NLTK данных
- Скачивания модели sentence-transformers

### 3. Запуск контейнеров

```bash
# Запускаем оба сервиса (pipeline и telegram_bot)
docker compose up -d

# Или запускаем только pipeline
docker compose up -d pipeline

# Или запускаем только telegram_bot
docker compose up -d telegram_bot
```

### 4. Проверка статуса контейнеров

```bash
# Смотрим статус всех контейнеров
docker compose ps

# Должен быть вывод вида:
# NAME                      IMAGE                STATUS
# alphapulse_pipeline       aialphapulse-pipeline  Up X minutes
# alphapulse_telegram_bot   aialphapulse-pipeline  Up X minutes
```

---

## ✅ Проверка работы

### 1. Проверка логов

```bash
# Логи pipeline воркера
docker compose logs pipeline

# Логи telegram бота
docker compose logs telegram_bot

# Логи обоих сервисов
docker compose logs

# Следить за логами в реальном времени
docker compose logs -f pipeline
docker compose logs -f telegram_bot

# Последние 100 строк логов
docker compose logs --tail=100 pipeline
```

### 2. Проверка healthcheck

```bash
# Статус healthcheck
docker compose ps

# Детальная информация о контейнере
docker inspect alphapulse_pipeline | grep -A 10 Health
```

### 3. Проверка работы pipeline

```bash
# Смотрим логи pipeline - должны быть сообщения о циклах обработки
docker compose logs pipeline | tail -50

# Ожидаемый вывод:
# 🚀 PIPELINE WORKER
# 📊 Интервал проверки: 300с
# 📝 Нормализация...
# 🔍 Дедупликация...
# 🤖 LLM анализ...
# ✅ ЦИКЛ ЗАВЕРШЕН
```

### 4. Проверка работы Telegram бота

```bash
# Смотрим логи бота
docker compose logs telegram_bot | tail -50

# Проверяем бота в Telegram
# Откройте Telegram и найдите вашего бота
# Отправьте команду /start
# Должен прийти ответ с приветствием
```

### 5. Проверка подключения к БД

```bash
# Заходим в контейнер pipeline
docker exec -it alphapulse_pipeline bash

# Проверяем подключение к БД
python3 -c "
from src.database import get_db_connection
conn = get_db_connection()
conn.connect()
print('✅ Подключение к БД успешно')
conn.close()
"

# Выходим из контейнера
exit
```

---

## 📊 Мониторинг и логи

### 1. Просмотр логов

```bash
# Все логи
docker compose logs

# Логи конкретного сервиса
docker compose logs pipeline
docker compose logs telegram_bot

# Логи в реальном времени
docker compose logs -f

# Логи за последний час
docker compose logs --since 1h pipeline

# Логи с фильтрацией
docker compose logs pipeline | grep "ERROR"
docker compose logs pipeline | grep "✅"
```

### 2. Статистика контейнеров

```bash
# Использование ресурсов
docker stats

# Или для конкретного контейнера
docker stats alphapulse_pipeline
docker stats alphapulse_telegram_bot
```

### 3. Проверка дискового пространства

```bash
# Использование диска Docker
docker system df

# Очистка неиспользуемых ресурсов (осторожно!)
docker system prune -a  # Удалит все неиспользуемые образы и контейнеры
```

### 4. Логи в файловой системе

```bash
# Логи сохраняются в ./logs/
ls -lh logs/

# Просмотр логов
tail -f logs/*.log
```

---

## 🔄 Обновление

### 1. Обновление кода

```bash
# Останавливаем контейнеры
docker compose down

# Получаем последние изменения
git pull origin master

# Пересобираем образы
docker compose build --no-cache

# Запускаем заново
docker compose up -d
```

### 2. Обновление без простоя (zero-downtime)

```bash
# Собираем новый образ
docker compose build pipeline

# Перезапускаем только pipeline (бот продолжит работать)
docker compose up -d --no-deps pipeline

# Проверяем что новый контейнер работает
docker compose logs -f pipeline
```

### 3. Откат к предыдущей версии

```bash
# Останавливаем контейнеры
docker compose down

# Переключаемся на предыдущую версию
git checkout <previous-commit-hash>

# Пересобираем и запускаем
docker compose build
docker compose up -d
```

---

## 🔧 Устранение неполадок

### 1. Контейнер не запускается

```bash
# Проверяем логи ошибок
docker compose logs pipeline

# Проверяем статус контейнера
docker compose ps

# Проверяем конфигурацию
docker compose config

# Перезапускаем контейнер
docker compose restart pipeline
```

### 2. Ошибки подключения к БД

```bash
# Проверяем доступность БД из контейнера
docker exec -it alphapulse_pipeline bash
ping $POSTGRES_HOST
telnet $POSTGRES_HOST $POSTGRES_PORT
exit

# Проверяем переменные окружения в контейнере
docker exec alphapulse_pipeline env | grep POSTGRES

# Проверяем .env файл
cat .env | grep POSTGRES
```

### 3. Контейнер перезапускается в цикле

```bash
# Смотрим логи последнего запуска
docker compose logs --tail=100 pipeline

# Проверяем healthcheck
docker inspect alphapulse_pipeline | grep -A 20 Health

# Проверяем ресурсы
docker stats alphapulse_pipeline
```

### 4. Ошибки с моделями или зависимостями

```bash
# Пересобираем образ с нуля
docker compose build --no-cache pipeline

# Заходим в контейнер и проверяем установку
docker exec -it alphapulse_pipeline bash
python3 -c "import sentence_transformers; print('OK')"
python3 -c "import faiss; print('OK')"
exit
```

### 5. Очистка и переустановка

```bash
# Останавливаем и удаляем контейнеры
docker compose down

# Удаляем образы
docker compose rm -f

# Удаляем volumes (осторожно - удалит данные!)
docker compose down -v

# Пересобираем с нуля
docker compose build --no-cache
docker compose up -d
```

---

## 📝 Полезные команды

### Управление контейнерами

```bash
# Остановка
docker compose stop

# Запуск
docker compose start

# Перезапуск
docker compose restart

# Остановка и удаление
docker compose down

# Остановка и удаление с volumes
docker compose down -v
```

### Информация

```bash
# Статус всех сервисов
docker compose ps

# Конфигурация (проверка)
docker compose config

# Версии
docker compose version
docker --version
```

### Вход в контейнер

```bash
# Вход в контейнер pipeline
docker exec -it alphapulse_pipeline bash

# Выполнение команды в контейнере
docker exec alphapulse_pipeline python3 -c "print('test')"
```

---

## 🔐 Безопасность

### 1. Защита .env файла

```bash
# Устанавливаем правильные права
chmod 600 .env

# Проверяем что файл не в git
grep .env .gitignore
```

### 2. Firewall

```bash
# Рекомендуется настроить firewall для PostgreSQL
# Разрешаем доступ только с localhost
sudo ufw allow from 127.0.0.1 to any port 5432
```

### 3. Резервное копирование

```bash
# Создаем бэкап БД
pg_dump -h localhost -U admin -d alphapulse > backup_$(date +%Y%m%d).sql

# Или через docker если PostgreSQL в контейнере
docker exec postgres_container pg_dump -U admin alphapulse > backup.sql
```

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи: `docker compose logs`
2. Проверьте статус: `docker compose ps`
3. Проверьте конфигурацию: `docker compose config`
4. Проверьте .env файл: убедитесь что все переменные установлены

---

## ✅ Чеклист развертывания

- [ ] Установлен Docker и Docker Compose
- [ ] Проект склонирован из репозитория
- [ ] Создан и настроен .env файл
- [ ] Установлены правильные права на .env (600)
- [ ] PostgreSQL доступен и работает
- [ ] Образы успешно собраны (`docker compose build`)
- [ ] Контейнеры запущены (`docker compose up -d`)
- [ ] Проверены логи (`docker compose logs`)
- [ ] Pipeline обрабатывает статьи (видно в логах)
- [ ] Telegram бот отвечает на команды
- [ ] Healthcheck показывает "healthy"
- [ ] Настроен мониторинг и логирование

---

**Удачного развертывания! 🚀**

