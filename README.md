# AiAlphaPulse - MVP системы поиска и оценки горячих новостей

## Описание проекта

**AiAlphaPulse** - это MVP (Minimum Viable Product) сервиса для поиска и оценки "горячих новостей". Система анализирует новостной поток, определяет наиболее значимые события и генерирует черновики публикаций с оценкой "горячести".

## Архитектура MVP

### 🎯 Основная цель
Реализовать сервис «поиска и оценки горячих новостей», который на вход получает временное окно (например, последние 24 часа) и возвращает топ-K событий с оценкой "горячести" и черновиком публикации.

### 📊 Выходные данные
Для каждой горячей новости система генерирует:

- **headline** — краткий заголовок
- **hotness** ∈ [0,1] — оценка «горячести»
- **why_now** — 1–2 фразы, почему важно сейчас (новизна, подтверждения, масштаб затронутых активов)
- **entities** — компании/тикеры/страны/сектора
- **sources** — 3–5 проверяемых ссылок (оригинал, подтверждение, апдейт)
- **timeline** — ключевые метки времени (первое сообщение → подтверждение → уточнение)
- **draft** — черновик для поста/заметки (заголовок, лид-абзац, 3 буллета, цитата/сноска)
- **dedup_group** — ID кластера дубликатов/перепечаток

## Текущая реализация

### 🔧 Компоненты системы

#### 1. **Парсер новостей** (внешний)
- Собирает новости из различных источников
- Сохраняет в базу данных `data/rss_articles.db`
- Структура: `articles` (id, title, content, link, source, published, etc.)

#### 2. **Модуль нормализации данных** (`src/normalization/`)
- **Очистка и нормализация**: HTML теги, кодировка, лемматизация
- **Фильтрация мусора**: короткие статьи, спам-паттерны
- **Определение языка**: автоматическое определение языка текста
- **Извлечение сущностей**: компании, тикеры, страны
- **Оценка качества**: расчет балла качества статьи (0-1)

#### 3. **База данных нормализованных данных**
- Таблица `normalized_articles` с очищенными данными
- Поля: `original_id`, `title`, `content`, `language_code`, `entities_json`, `quality_score`, etc.
- Система предотвращения дубликатов

#### 4. **Дедупликация и кластеризация (src/dedup/)**

- Таблицы:

  * vectors — эмбеддинги для normalized_articles.id (модель SentenceTransformers),

  * story_clusters — агрегаты сюжета (headline, first/last_time, домены, ссылки, факторы, hotness),

  * cluster_members — связь документ ↔ кластер с time_utc, site, url,

  * dedup_state — прогресс инкрементальной обработки.

- Индекс: FAISS (cosine, IndexFlatIP), поддержка инкремента.

- Решение по соседям: два порога cosine

  * τ_dup = 0.95 → явный дубль,

  * τ_story = 0.89 → тот же сюжет при совпадении языка и в окне ±48h.

- Ссылки в сюжете: earliest / strongest / latest (strongest выбирается по весу домена).

#### 5. **Hotness‑скоринг (бейзлайн)**

Вычисляется при каждом пополнении кластера (см. recompute_scores()):

- hotness = 0.30*novelty + 0.20*source + 0.20*velocity + 0.15*confirmation + 0.10*materiality + 0.05*breadth

- novelty: 1.0 если first_time ≤ 6h, иначе 0.3

- source: макс. вес домена (напр. sec.gov=1.0, reuters/bloomberg=0.9, ft/wsj=0.85, cnbc=0.8, иначе 0.5)

- velocity: sigmoid(log(doc_count+1))

- confirmation: min(#уникальных_доменов / 4, 1.0)

- materiality: 0.3 (плейсхолдер — можно поднять при наличии релевантных сущностей)

- breadth: 0.0 (плейсхолдер — широта охвата по entities)

## Структура проекта

```
AiAlphaPulse/
├── src/
│ ├── normalization/
│ │   ├── normalizer.py
│ │   ├── database_schema.py
│ │   └── process_articles.py
│ └── dedup/
│     ├── schema.py
│     ├── embedder.py
│     ├── index_faiss.py
│     ├── logic.py
│     └── runner.py
├── data/
│   └── rss_articles.db        # База данных с новостями
├── process_articles.py         # Скрипт обработки статей
├── export_articles.py          # Скрипт экспорта данных
├── view_articles.py            # Скрипт просмотра данных
├── requirements.txt            # Зависимости Python
├── articles_latest.json        # Тестовые данные (100 статей)
└── README.md                   # Документация
```

## Возможности системы

### ✅ Реализовано

1. **Нормализация данных**
   - Очистка HTML тегов и декодирование entities
   - Определение языка текста (ru, en, etc.)
   - Фильтрация спама и мусорных статей
   - Извлечение сущностей (компании, тикеры)
   - Оценка качества статьи

2. **Управление данными**
   - Предотвращение дубликатов при обработке
   - Эффективная пакетная обработка
   - Экспорт в JSON для дальнейшего анализа
   - Статистика и мониторинг

3. **Инструменты**
   - Командная строка для всех операций
   - Фильтрация по языку, качеству, источнику
   - Просмотр и анализ данных

### 🚧 В разработке (следующие этапы)

1. **Алгоритм оценки "горячести"**
   - Анализ временных паттернов
   - Кластеризация дубликатов
   - Расчет релевантности и новизны

2. **Генерация контента**
   - Создание заголовков и лид-абзацев
   - Извлечение ключевых цитат
   - Формирование timeline событий

3. **API и интерфейс**
   - REST API для интеграции
   - Веб-интерфейс для мониторинга
   - Настройка параметров анализа

## Установка и запуск

### 1. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 2. нормализация
```bash
# Обработка статей из базы данных
python process_articles.py --unprocessed

# Просмотр статистики
python process_articles.py --stats
```

2.1. Просмотр результатов
```bash
# Статистика нормализованных данных
python view_articles.py --stats

# Просмотр лучших статей
python view_articles.py --limit 10 --min-quality 0.9
```

2.2. Экспорт данных
```bash
# Экспорт высококачественных статей
python export_articles.py --high-quality --output hot_news.json
```
### 3. Построить кластеры и hotness (инкрементально)
```
python -m src.dedup.runner --db data/rss_articles.db
```
Результат: заполнены vectors, story_clusters, cluster_members. В консоли — количество обработанных статей.

3.1 Экспорт топ‑сюжетов (опционально)

```
python -m scripts.export_topk --db data/rss_articles.db --out radar_top.json --top-k 10 --window-hours 48
```
На выходе radar_top.json с карточками: dedup_group, headline, hotness, sources (earliest/strongest/latest), timeline, domains, factors, doc_count
## Алгоритм работы

### Этап 1: Нормализация
1. Загрузка статей из базы данных
2. Очистка HTML и нормализация текста
3. Определение языка и извлечение сущностей
4. Фильтрация спама и оценка качества
5. Сохранение в нормализованную таблицу

### Этап 2: Анализ (планируется)
1. Кластеризация похожих новостей
2. Анализ временных паттернов
3. Расчет релевантности и новизны
4. Оценка "горячести" события

### Этап 3: Генерация (планируется)
1. Создание заголовков и описаний
2. Формирование timeline событий
3. Генерация черновиков публикаций
4. Подготовка метаданных

## Технические детали

### Зависимости
- `beautifulsoup4` - очистка HTML
- `langdetect` - определение языка
- `nltk` - обработка текста
- `sqlite3` - база данных

### Производительность
- Обработка ~100 статей за 0.4 секунды
- Эффективная система предотвращения дубликатов
- Пакетная обработка для больших объемов

### Качество данных
- Средний балл качества: 0.96
- Фильтрация спама: ~30% статей отфильтровано
- Поддержка русского и английского языков

## Следующие шаги

1. **Реализация алгоритма оценки "горячести"**
2. **Кластеризация дубликатов и похожих новостей**
3. **Генерация контента и черновиков**
4. **Создание API для интеграции**
5. **Разработка веб-интерфейса**

## Контакты

Проект находится в активной разработке. Для вопросов и предложений обращайтесь к команде разработки.
